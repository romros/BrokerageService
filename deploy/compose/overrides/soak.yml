# Override per WS soak: pipeline candles actiu (fake feed, sense xarxa)
#
# Ús:
#   docker compose -f docker-compose.yml -f deploy/compose/overrides/soak.yml up -d brokerage
#   ./scripts/run_soak.sh 15 ws

services:
  brokerage:
    environment:
      - VENUE=lighter
      - MODE=paper
      - USE_FAKE_PRICE_FEED=1
      - SYMBOLS=ETH,BTC
