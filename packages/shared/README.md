# packages/shared

**Propòsit:** Codi compartit entre serveis (realtime_datalayer, historical_datalayer, trading_service).

---

## Contingut previst (encara no migrat)

- `foundation/config/constants.py` — TZ, timeframe, limits, paths
- `domain/*` — ports, errors
- models comuns (OHLCV, candles, etc.)
- helpers de logging, config

---

## Status

**Scaffold:** placeholder. El codi actual viu a `application/`, `foundation/`, `domain/`, `infrastructure/`.  
Migració en tasques posteriors.
