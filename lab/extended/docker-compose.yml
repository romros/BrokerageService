version: '3.8'

services:
  extended-cli:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: extended-cli
    volumes:
      - .:/app
      - ~/.env:/app/.env:ro
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true
    command: /bin/bash
