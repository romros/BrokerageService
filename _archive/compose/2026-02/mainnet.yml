# ARXIVAT 2026-02. Reemplaçament: deploy/compose/overrides/soak.yml (ws) o data-layer.yml
# Override per WS soak MAINNET (real feed, sense fake)

services:
  brokerage:
    environment:
      - VENUE=lighter
      - MODE=paper
      - MARKET_DATA_ENV=mainnet
      - USE_FAKE_PRICE_FEED=0
      - SYMBOLS=ETH,BTC
      - LIGHTER_SYMBOLS=ETH,BTC
